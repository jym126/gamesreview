import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.gameReview',
  appName: 'GameRieview',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
